[gd_scene load_steps=4 format=2]

[ext_resource path="res://data/sprites/bush.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Area2D

# Declare member variables here.

#Scenes
var leaf_scene = preload(\"res://data/scenes/Leaf.tscn\")

#Nodes
onready var map = self.get_parent()

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

func _collect():
	# Preparing the leaf to create the leaf
	var leaf_instance = leaf_scene.instance()
	leaf_instance.picked_up = true
	
	# Leaf creating
	map.add_child(leaf_instance)
	
	# Bush destroying
	self.queue_free()
	
	# Returning leaf so it can be put into player's inventory
	return leaf_instance
"

[sub_resource type="RectangleShape2D" id=2]
extents = Vector2( 6.5, 6 )

[node name="Bush" type="Area2D" groups=[
"material source",
]]
script = SubResource( 1 )

[node name="Sprite" type="Sprite" parent="."]
texture = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2( -0.5, 0 )
shape = SubResource( 2 )
