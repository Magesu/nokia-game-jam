[gd_scene load_steps=4 format=2]

[ext_resource path="res://data/sprites/stone_pile.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Area2D

# Declare member variables here.

#Scenes
var rock_scene = preload(\"res://data/scenes/Rock.tscn\")

#Nodes
onready var map = self.get_parent()

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

func _collect():
	# Preparing the rock to create the rock
	var rock_instance = rock_scene.instance()
	rock_instance.picked_up = true
	
	# Rock creating
	map.add_child(rock_instance)
	
	# Stone pile destroying
	self.queue_free()
	
	# Returning rock so it can be put into player's inventory
	return rock_instance
"

[sub_resource type="RectangleShape2D" id=2]
extents = Vector2( 8, 6.5 )

[node name="Stone Pile" type="Area2D" groups=[
"material source",
]]
collision_layer = 4
script = SubResource( 1 )

[node name="Sprite" type="Sprite" parent="."]
position = Vector2( 0, -2 )
texture = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2( 0, -0.5 )
shape = SubResource( 2 )
