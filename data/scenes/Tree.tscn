[gd_scene load_steps=4 format=2]

[ext_resource path="res://data/sprites/tree.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Area2D

# Declare member variables here.

#Scenes
var wood_scene = preload(\"res://data/scenes/Wood.tscn\")

#Nodes
onready var map = self.get_parent()

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

func _collect():
	# Preparing the wood to create the wood
	var wood_instance = wood_scene.instance()
	wood_instance.picked_up = true
	
	# Wood creating
	map.add_child(wood_instance)
	
	# Tree destroying
	self.queue_free()
	
	# Returning wood so it can be put into player's inventory
	return wood_instance
"

[sub_resource type="RectangleShape2D" id=2]
extents = Vector2( 3.5, 12.5 )

[node name="Tree" type="Area2D" groups=[
"material source",
]]
collision_layer = 4
script = SubResource( 1 )

[node name="Sprite" type="Sprite" parent="."]
texture = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2( -0.5, 0.5 )
shape = SubResource( 2 )
